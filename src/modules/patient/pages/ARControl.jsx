import { Box, Card, CardContent, Typography } from '@mui/material';
import PageHeader from '@components/common/PageHeader';

const ARControl = () => (
  <Box>
    <PageHeader
      title="A/R Control"
      subtitle="Accounts receivable control and management."
    />
    <Card>
      <CardContent>
        <Typography variant="body2" color="text.secondary">
          A/R Control module — content coming soon.
        </Typography>
      </CardContent>
    </Card>
  </Box>
);

export default ARControl;
