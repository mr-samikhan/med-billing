import { Box, Card, CardContent, Typography } from '@mui/material';
import PageHeader from '@components/common/PageHeader';

const StatementsSetup = () => (
  <Box>
    <PageHeader
      title="Statements"
      subtitle="Configure statement templates and settings."
    />
    <Card>
      <CardContent>
        <Typography variant="body2" color="text.secondary">
          Statements module — content coming soon.
        </Typography>
      </CardContent>
    </Card>
  </Box>
);

export default StatementsSetup;
