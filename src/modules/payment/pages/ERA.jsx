import { Box, Card, CardContent, Typography } from '@mui/material';
import PageHeader from '@components/common/PageHeader';

const ERA = () => (
  <Box>
    <PageHeader
      title="ERA"
      subtitle="Electronic Remittance Advice processing."
    />
    <Card>
      <CardContent>
        <Typography variant="body2" color="text.secondary">
          ERA module — content coming soon.
        </Typography>
      </CardContent>
    </Card>
  </Box>
);

export default ERA;
